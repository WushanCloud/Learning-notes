#include<stdio.h>

int Func(char num)
{
	int sum = 0;
	while (num)
	{
		int tmp = 0;
		tmp = num % 10;
		if (tmp >= '1' && tmp<='9')
		{

		}
	}
}
//十六进制转换十进制
int main()
{

	printf("%d",Func(nums));
	return 0;
}